#include "main.h"
#include<stdio.h>
/**
 * mul - multiplies a to b
 *
 * @a: function parameter
 *
 * @b: function parameter
 * Return: mul
 *
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);

	return (mul);
}
